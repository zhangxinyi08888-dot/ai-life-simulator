# 财务支出生命周期发布豁免记录

日期：2026-08-17

分支：`codex/financial-expense-lifecycle`

结论：允许进入小流量预发布；**2/2/1 未重新认证**。

## 豁免边界

本次不是新的五路线发布认证。此前 v19 的 2/2/1 结果及其失败结论不被本记录覆盖或替代。本记录只证明三个已知发布阻断在静态门禁与两条短真实路线中得到修复：

1. Education 已完成入职语义不再被误判为“只有未来 offer”，避免薪资事实拆分造成可见暂停。
2. 可局部收敛的关系权威冲突和债务叙事冲突不再默认触发整章重生成；候选 Patch 发布开关仍保持关闭。
3. 结局海报为标题、摘要、时间线与结语增加文案预算、一次定向修复及 CSS 溢出保护。

## 静态与 R2 门禁

以下命令均在当前工作树实现上通过：

- `pnpm lint`：通过。
- `pnpm test`：949/949 通过。
- `pnpm build`：通过；仅保留既有 bundle size 警告。
- `pnpm test:test-process`：23/23 通过。
- `pnpm test:financial-production-blockers`：通过。

## 短真实路线证据

证据目录位于 Git 工作区之外：

`/Users/zz/Documents/new life/ai-life-test-evidence/report-invitation-browser/2026-08-17T16-05-15+08-00-financial-waiver-short-routes-v2`

该目录由同一 explore 候选启动，浏览器运行身份与候选清单一致：

- Education：全新真实 AI 路线，5/5 节点完整，无可见暂停。
- Venture：全新真实 AI 路线，5/5 节点完整，无可见暂停。
- 合计 10 个节点；单次完整生成率 100%（10/10），目标为至少 90%。
- 财务不变量失败 0；fallback 0；阻断节点时间推进 0；拒绝节点期间计提 0。
- 企业经营流误入个人账本 0；成年仅最低支出线持续月份 0。

短路线不进入结局页，因此机器审计中的海报缺失/重复计数不适用于本次短探索。海报修复由静态回归覆盖；未把短路线包装成终局证据。

## 已知限制

- 未重新运行全新 2/2/1，不能声明发布候选已重新认证。
- 两条 5 节点路线只能证明已知 Education/Venture 回归及生成链路，没有覆盖完整人生终局分布。
- 短样本的 Accepted 财务叙事覆盖率为 83.3333%，只作诊断，不作为新的五路线覆盖率结论。
- 候选 Patch 功能仍为 `false`，本次不借机放宽发布功能开关。

## 预发布与回滚

仅允许小流量预发布。上线后重点监控：

- 用户可见生成暂停；
- 海报标题、摘要、时间线或结语溢出、截断、空白；
- Education 入职薪资再次被拆分拒绝；
- Venture 债务叙事冲突导致整章重生成。

任一可见暂停或海报异常出现时立即回滚到发布前提交，停止扩大流量，并保留失败路线、首次失败节点和运行身份用于复现。
