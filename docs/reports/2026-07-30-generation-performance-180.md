# 候选节点 Patch 机制真实模型性能验收

> 2026-07-31 复核：本报告保留为原始历史证据，但旧口径把“Patch 可解析并合并”记作成功。按新的有效口径，只有 1/7 Patch 最终避免了完整重生成，有效率为 14.29%，因此本报告不能继续作为 Patch 开启状态的发布证明。生产默认已调整为关闭 Candidate Patch；重新启用前需生成新的同提交证据。

## 结论

通过。候选版本完成 3 类人物 × 3 轮 × 20 节点，共 180 个真实 AI 浏览器节点；性能、调用预算、用户可见暂停和权威财务审计均满足发布门禁。

## 方法

- 人物：普通职业、关系/健康交错、财务/创业。
- 每类 3 轮，每轮 20 节点，共 9 条完整路线、180 个节点。
- 候选计时为用户点击选项至新节点可交互的页面耗时，包含模型调用、校验、局部修复和提交，因此比只计算模型请求跨度更保守。
- 合入前控制组取自 `2026-07-29T13-51-03+08-00-ordinary-career-20` 的 main 结果：P90 31,948 ms，完整重生成节点率 30%，用户可见暂停 0。
- 原始路线与机器汇总保存在工作区外的发布证据目录；大型 JSON 不进入 PR。

## 结果

| 门禁 | 控制组 / 阈值 | 候选结果 | 状态 |
|---|---:|---:|---|
| 节点数 | 3 × 3 × 20 | 180 | 通过 |
| P90 | ≤ 35,142.8 ms | 22,472 ms | 通过 |
| 完整重生成节点率 | ≤ 35% | 5.56%（10/180） | 通过 |
| 仅一次完整生成 | ≥ 90% | 94.44%（170/180） | 通过 |
| 单事务模型 Patch | ≤ 1 | 1 | 通过 |
| Candidate Patch 成功率 | ≥ 50% | 57.14%（4/7） | 通过 |
| 用户可见暂停 | 0 | 0 | 通过 |
| 未分类模型调用 | 0 | 0 | 通过 |
| 权威事实回归 | 0 | 0 | 通过 |

候选 P50 为 15,373 ms，最大单节点耗时为 41,279 ms。Proposal repair 共 87 次；它与 Candidate Patch 共用单事务最多一次的模型 Patch 预算，180 个节点中没有事务超过预算。

## 动态财务审计

以下阻断项均为 0：无解释债务跳变、债务守恒失败、开局编造账户、资产汇总不一致、已知利率漏计息、无依据声称还清、金额占位符、孤立金额、长浮点、邀请串线、公司经营流混入个人账本、用户可见内部账本文本。

检测到 2 个 `FINANCIAL_COMPLETION_ROLLBACK` 叙事降级节点，均有修复记录，且没有向用户展示内部账本措辞，因此不计为权威事实回归。

## 代码门禁

- 全量测试：525/525 通过。
- 财务生产阻断套件（含 P0、M5、M7）：通过。
- TypeScript：通过。
- 生产构建：通过；仅保留既有的大 chunk 警告。
